import 'package:flutter/material.dart';
import 'package:sample_flutter_app/album/screen_1/screen_1.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget{
  @override
  Widget build(BuildContext buildContext) {
    return MaterialApp(
      home: WelcomePage(),
    );
  }}